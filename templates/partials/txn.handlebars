public {{name}}Tx({{> param_types inputs=inputs}}): TransactionObject<TypedTransactionReceipt> {
  return this.web3Contract.methods.{{name}}(
    {{#each inputs}}
    {{name}}.toString(){{#unless @last}},{{/unless}}
    {{/each}}
  );
}
