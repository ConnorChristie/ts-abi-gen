public {{name}}Tx({{> param_types inputs=inputs}}): DeferredTransactionWrapper<{{#payable}}IPayableTxParams{{/payable}}{{^payable}}Tx{{/payable}}> {
  return new DeferredTransactionWrapper<{{#payable}}IPayableTxParams{{/payable}}{{^payable}}Tx{{/payable}}>(this, '{{name}}', [
    {{#each inputs}}
    {{name}}{{#unless @last}},{{/unless}}
    {{/each}}
  ]);
}
